# ============================================
# .zshrc.local - Machine-specific settings
# ============================================
# このファイルを ~/.zshrc.local にコピーして使用してください
# cp ~/dotfiles/.zshrc.local.example ~/.zshrc.local

# --------------------------------------------
# mise (polyglot version manager)
# --------------------------------------------
# Node.js, Python, Ruby などを統一管理
# https://mise.jdx.dev/
if command -v mise 1>/dev/null 2>&1; then
    eval "$(mise activate zsh)"
fi

# Usage:
#   mise use node@20      # Node.js 20をインストール＆有効化
#   mise use python@3.12  # Python 3.12をインストール＆有効化
#   mise use ruby@3.3     # Ruby 3.3をインストール＆有効化
#   mise list             # インストール済みバージョン一覧
#   mise current          # 現在のバージョン表示

# --------------------------------------------
# pnpm
# --------------------------------------------
export PNPM_HOME="$HOME/Library/pnpm"
case ":$PATH:" in
    *":$PNPM_HOME:"*) ;;
    *) export PATH="$PNPM_HOME:$PATH" ;;
esac

# --------------------------------------------
# API Keys / Secrets (example)
# --------------------------------------------
# export GITHUB_TOKEN="your_token_here"
# export OPENAI_API_KEY="your_key_here"

# --------------------------------------------
# Custom PATH additions
# --------------------------------------------
# export PATH="$HOME/bin:$PATH"
